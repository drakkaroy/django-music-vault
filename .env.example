# --- Django ---
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# --- Database ---
# sqlite3 (default, zero config) or postgresql
DB_ENGINE=postgresql
DB_NAME=django
DB_USER=django
DB_PASSWORD=django
DB_HOST=localhost
DB_PORT=5432

# --- Spotify ---
# Same app credentials power both flows:
#  - client-credentials: search/autofill, no user login needed
#  - Authorization Code: the Play button, needs each user to connect their
#    account. In your Spotify Dashboard app, add this exact Redirect URI:
#    http://localhost:8000/spotify/callback/
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret
